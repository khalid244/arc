# Arc Core - Minimal Production Dependencies

# High-performance data processing
polars==1.34.0
pyarrow==21.0.0
numpy==2.3.3
pytz==2024.2  # Required by DuckDB for timezone-aware timestamps in existing Parquet files

# Fast async I/O
aiofiles==25.1.0

# API framework
fastapi==0.119.0
uvicorn[standard]==0.37.0
gunicorn==23.0.0
slowapi==0.1.9  # Rate limiting

# Performance optimizations
uvloop==0.21.0  # 2-4x faster event loop (Cython-based)
httptools==0.6.4  # 40% faster HTTP parser
orjson==3.10.13  # 20-50% faster JSON serialization (Rust + SIMD)

# Storage and query engine
# Note: boto3, aiobotocore, and botocore versions must be compatible
boto3
aiobotocore
s3fs==2024.12.0
google-cloud-storage==3.4.1
duckdb==1.4.1
minio==7.2.18

# Config and validation
toml==0.10.2
pyyaml==6.0.2
pydantic==2.12.0
python-dotenv==1.1.1

# Performance monitoring
psutil==7.1.0

# HTTP
python-multipart==0.0.6
httpx==0.28.1

# Binary protocols
msgpack==1.1.2

# JSON processing
jsonpath-ng==1.7.0

# SQL parsing
sqlparse==0.5.3

# Security
bcrypt==4.2.1  # Secure password/token hashing

# REMOVED: Database clients for importers/exporters (moved to external tools)
# influxdb, influxdb-client, psycopg2-binary, kafka-python
